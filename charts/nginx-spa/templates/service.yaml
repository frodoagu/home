apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-spa.fullname" . }}
  labels:
    {{- include "nginx-spa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "nginx-spa.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
