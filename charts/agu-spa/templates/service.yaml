apiVersion: v1
kind: Service
metadata:
  name: {{ include "agu-spa.fullname" . }}
  labels:
    {{- include "agu-spa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "agu-spa.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
