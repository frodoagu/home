# Default values for argocd wrapper chart.
# All keys under argo-cd are forwarded to the upstream argo-cd chart.
# Full reference: https://github.com/argoproj/argo-helm/blob/main/charts/argo-cd/values.yaml

argo-cd:
  # -- Expose Argo CD server via Traefik ingress
  server:
    ingress:
      enabled: true
      ingressClassName: traefik
      hostname: argocd.home.agu.com.ar
      tls: true
      annotations:
        traefik.ingress.kubernetes.io/router.tls.certresolver: letsencrypt
        traefik.ingress.kubernetes.io/router.entrypoints: websecure

    # -- Disable TLS on the server when terminating TLS at the ingress layer
    extraArgs:
      - --insecure

  # -- Use a single replica for a home / RPi environment
  controller:
    replicas: 1

  repoServer:
    replicas: 1

  applicationSet:
    replicas: 1

  # -- Restrict resource usage for RPi
  global:
    image:
      tag: "v2.11.3"
