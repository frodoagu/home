apiVersion: v1
kind: Service
metadata:
  name: {{ include "shelly-proxy.fullname" . }}
  labels:
    {{- include "shelly-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "shelly-proxy.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
